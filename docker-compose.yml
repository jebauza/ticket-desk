services:
  postgres-db:
    image: postgres:16.15-alpine
    container_name: ticketdesk_db
    restart: always
    environment:
      POSTGRES_DB: ticketdesk
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432